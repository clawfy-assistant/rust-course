# Solutions 📚

This folder contains complete solutions for all exercises in the Rust course.

## Structure

```
solutions/
├── 01_basics/          - Variables, types, functions, loops
├── 02_ownership/       - Ownership, borrowing, slices
├── 03_structs_enums/   - Structs, enums, Option, Result
├── 04_collections/     - Vectors, HashMaps
├── 05_error_handling/  - Error handling patterns
├── 06_generics/        - Generic types and functions
├── 07_lifetimes/       - Lifetime annotations
├── 08_traits/          - Traits and trait bounds
├── 09_closures_iterators/ - Closures, iterators
├── 10_concurrency/     - Threads, channels, Arc, Mutex
├── 11_smart_pointers/  - Box, Rc, RefCell
└── 12_advanced/        - Unsafe, macros
```

## How to Use

1. **Try first**: Attempt the exercises in `src/lib.rs` on your own
2. **Check solutions**: Look here if you're stuck or want to compare
3. **Understand**: Read the explanations, not just copy code
4. **Run tests**: Verify with `cargo test` in each module

## Solution Format

Each solution includes:
- ✅ Complete working code
- 📝 Explanation of key concepts
- 💡 Tips and best practices
- 🔍 Common pitfalls to avoid

## Quick Reference

| Module | Key Concepts |
|--------|--------------|
| 01 | `let mut`, tuples, arrays, loops, recursion |
| 02 | `&T`, `&mut T`, slices, ownership rules |
| 03 | `struct`, `enum`, `Option`, `Result`, `match` |
| 04 | `Vec`, `HashMap`, iterators |
| 05 | `?` operator, custom errors |
| 06 | `<T>`, generic functions |
| 07 | Lifetime annotations `'<a>` |
| 08 | `trait`, `impl Trait`, trait bounds |
| 09 | Closures, `map`, `filter`, `collect` |
| 10 | `spawn`, `mpsc`, `Arc<Mutex>` |
| 11 | `Box`, `Rc`, `RefCell` |
| 12 | `unsafe`, macros |

---

**Note**: Solutions are for learning. Try the exercises yourself first! 🦀